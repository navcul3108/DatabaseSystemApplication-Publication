extends ../layout.pug

block variables
  -var title= "Profile"

block content
      .container
        .row
          .col-lg-4
            img.w-100(src="/images/avatar-default.png", alt="Image not found")
          .col-lg-8
            h2= user.profile.firstName + user.profile.lastName
            h3= user.profile.email
            if groups.length==0
              h4 Khách
            else
              each group in groups
                h4= group.profile.name